/**
 * Note: Gemini API calls are handled exclusively by the backend server (server.js)
 * to keep the API key safe and prevent client-side exposure.
 */
export { sendChatMessage } from './chatService';
